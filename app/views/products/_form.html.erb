<%= form_for(@product) do |f| %>

<div class="field">
	<%= f.label :name %><br />
	<%= f.text_field :name %>
</div>

<div class="field">
	<%= f.label :description %>
	<%= f.text_field :description %>
</div>

<div class="field">
	<%= f.label :price_in_cents %>
	<%= f.label :price_in_cents %>
</div>

<div class="action">
	<%= f.submit %>
</div>
<% end %>